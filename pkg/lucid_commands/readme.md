# Lucid's Console Commands

Like what the title says, all lucid console commands lives inside this and pre-loaded inside `internal/kernel/cli.go`

Note that the `var Commands` lives inside `struct.go`
